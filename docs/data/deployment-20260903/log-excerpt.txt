2026-08-25T08:10:16Z risepir-rpc mainnet: reconcile at block 25830840: 8 account(s) exact vs independent provider
2026-08-25T08:16:40Z risepir-rpc mainnet: reconcile at block 25830870: 8 account(s) exact vs independent provider
2026-08-25T08:23:00Z risepir-rpc mainnet: reconcile at block 25830900: 8 account(s) exact vs independent provider
2026-08-25T08:29:32Z risepir-rpc mainnet: reconcile at block 25830930: 8 account(s) exact vs independent provider
2026-08-25T08:35:44Z risepir-rpc mainnet: reconcile at block 25830960: 8 account(s) exact vs independent provider
2026-08-25T08:42:15Z risepir-rpc mainnet: reconcile at block 25830990: 8 account(s) exact vs independent provider
2026-08-25T08:42:30Z risepir-rpc mainnet: reconcile at block 25831020: 8 account(s) exact vs independent provider
2026-08-25T08:48:49Z risepir-rpc mainnet: reconcile at block 25831050: 8 account(s) exact vs independent provider
2026-08-25T08:55:15Z risepir-rpc mainnet: reconcile at block 25831080: 8 account(s) exact vs independent provider
2026-08-25T09:01:32Z risepir-rpc mainnet: reconcile at block 25831110: 8 account(s) exact vs independent provider
2026-08-25T09:07:52Z risepir-rpc mainnet: reconcile at block 25831140: 8 account(s) exact vs independent provider
2026-08-25T09:14:18Z risepir-rpc mainnet: reconcile at block 25831170: 8 account(s) exact vs independent provider
2026-08-25T09:20:41Z risepir-rpc mainnet: reconcile at block 25831200: 8 account(s) exact vs independent provider
2026-08-25T09:27:08Z risepir-rpc mainnet: reconcile at block 25831230: 8 account(s) exact vs independent provider
2026-08-25T09:33:30Z risepir-rpc mainnet: reconcile at block 25831260: 8 account(s) exact vs independent provider
2026-08-25T09:39:56Z risepir-rpc mainnet: reconcile at block 25831290: 8 account(s) exact vs independent provider
2026-08-25T09:46:18Z risepir-rpc mainnet: reconcile at block 25831320: 8 account(s) exact vs independent provider
2026-08-25T09:52:38Z risepir-rpc mainnet: reconcile at block 25831350: 8 account(s) exact vs independent provider
2026-08-25T09:58:58Z risepir-rpc mainnet: reconcile at block 25831380: 8 account(s) exact vs independent provider
2026-08-25T10:05:29Z risepir-rpc mainnet: reconcile at block 25831410: 8 account(s) exact vs independent provider
2026-08-25T10:11:49Z risepir-rpc mainnet: reconcile at block 25831440: 8 account(s) exact vs independent provider
2026-08-25T10:18:14Z risepir-rpc mainnet: reconcile at block 25831470: 8 account(s) exact vs independent provider
2026-08-25T10:24:33Z risepir-rpc mainnet: reconcile at block 25831500: 8 account(s) exact vs independent provider
2026-08-25T10:24:46Z risepir-rpc mainnet: reconcile at block 25831530: 8 account(s) exact vs independent provider
2026-08-25T10:31:08Z risepir-rpc mainnet: reconcile at block 25831560: 8 account(s) exact vs independent provider
2026-08-25T10:37:37Z risepir-rpc mainnet: reconcile at block 25831590: 8 account(s) exact vs independent provider
2026-08-25T10:43:58Z risepir-rpc mainnet: reconcile at block 25831620: 8 account(s) exact vs independent provider
2026-08-25T10:50:20Z risepir-rpc mainnet: reconcile at block 25831650: 8 account(s) exact vs independent provider
2026-08-25T10:56:41Z risepir-rpc mainnet: reconcile at block 25831680: 8 account(s) exact vs independent provider
2026-08-25T11:03:10Z risepir-rpc mainnet: reconcile at block 25831710: 8 account(s) exact vs independent provider
2026-08-25T11:09:26Z risepir-rpc mainnet: reconcile at block 25831740: 8 account(s) exact vs independent provider
2026-08-25T11:15:52Z risepir-rpc mainnet: reconcile at block 25831770: 8 account(s) exact vs independent provider
2026-08-25T11:22:17Z risepir-rpc mainnet: reconcile at block 25831800: 8 account(s) exact vs independent provider
2026-08-25T11:28:37Z risepir-rpc mainnet: reconcile at block 25831830: 8 account(s) exact vs independent provider
2026-08-25T11:34:59Z risepir-rpc mainnet: reconcile at block 25831860: 8 account(s) exact vs independent provider
2026-08-25T11:41:25Z risepir-rpc mainnet: reconcile at block 25831890: 8 account(s) exact vs independent provider
2026-08-25T11:47:47Z risepir-rpc mainnet: reconcile at block 25831920: 8 account(s) exact vs independent provider
2026-08-25T11:54:17Z risepir-rpc mainnet: reconcile at block 25831950: 8 account(s) exact vs independent provider
2026-08-25T12:00:33Z risepir-rpc mainnet: reconcile at block 25831980: 8 account(s) exact vs independent provider
2026-08-25T12:00:46Z risepir-rpc mainnet: reconcile at block 25832010: 8 account(s) exact vs independent provider
2026-08-25T12:07:09Z risepir-rpc mainnet: reconcile at block 25832040: 8 account(s) exact vs independent provider
2026-08-25T12:13:33Z risepir-rpc mainnet: reconcile at block 25832070: 8 account(s) exact vs independent provider
2026-08-25T12:19:56Z risepir-rpc mainnet: reconcile at block 25832100: 8 account(s) exact vs independent provider
2026-08-25T12:26:20Z risepir-rpc mainnet: reconcile at block 25832130: 8 account(s) exact vs independent provider
2026-08-25T12:32:42Z risepir-rpc mainnet: reconcile at block 25832160: 8 account(s) exact vs independent provider
2026-08-25T12:39:05Z risepir-rpc mainnet: reconcile at block 25832190: 8 account(s) exact vs independent provider
2026-08-25T12:45:28Z risepir-rpc mainnet: reconcile at block 25832220: 8 account(s) exact vs independent provider
2026-08-25T12:51:56Z risepir-rpc mainnet: reconcile at block 25832250: 8 account(s) exact vs independent provider
2026-08-25T12:58:18Z risepir-rpc mainnet: reconcile at block 25832280: 8 account(s) exact vs independent provider
2026-08-25T13:04:40Z risepir-rpc mainnet: reconcile at block 25832310: 8 account(s) exact vs independent provider
2026-08-25T13:11:02Z risepir-rpc mainnet: reconcile at block 25832340: 8 account(s) exact vs independent provider
2026-08-25T13:17:29Z risepir-rpc mainnet: reconcile at block 25832370: 8 account(s) exact vs independent provider
2026-08-25T13:23:51Z risepir-rpc mainnet: reconcile at block 25832400: 8 account(s) exact vs independent provider
2026-08-25T13:30:13Z risepir-rpc mainnet: reconcile at block 25832430: 8 account(s) exact vs independent provider
2026-08-25T13:36:38Z risepir-rpc mainnet: reconcile at block 25832460: 8 account(s) exact vs independent provider
2026-08-25T13:42:57Z risepir-rpc mainnet: reconcile at block 25832490: 8 account(s) exact vs independent provider
2026-08-25T13:43:13Z risepir-rpc mainnet: reconcile at block 25832520: 8 account(s) exact vs independent provider
2026-08-25T13:49:37Z risepir-rpc mainnet: reconcile at block 25832550: 8 account(s) exact vs independent provider
2026-08-25T13:56:03Z risepir-rpc mainnet: reconcile at block 25832580: 8 account(s) exact vs independent provider
2026-08-25T14:02:22Z risepir-rpc mainnet: reconcile at block 25832610: 8 account(s) exact vs independent provider
2026-08-25T14:08:49Z risepir-rpc mainnet: reconcile at block 25832640: 8 account(s) exact vs independent provider
2026-08-25T14:15:08Z risepir-rpc mainnet: reconcile at block 25832670: 8 account(s) exact vs independent provider
2026-08-25T14:21:33Z risepir-rpc mainnet: reconcile at block 25832700: 8 account(s) exact vs independent provider
2026-08-25T14:27:58Z risepir-rpc mainnet: reconcile at block 25832730: 8 account(s) exact vs independent provider
2026-08-25T14:34:22Z risepir-rpc mainnet: reconcile at block 25832760: 8 account(s) exact vs independent provider
2026-08-25T14:40:42Z risepir-rpc mainnet: reconcile at block 25832790: 8 account(s) exact vs independent provider
2026-08-25T14:47:05Z risepir-rpc mainnet: reconcile at block 25832820: 8 account(s) exact vs independent provider
2026-08-25T14:53:24Z risepir-rpc mainnet: reconcile at block 25832850: 8 account(s) exact vs independent provider
2026-08-25T14:59:54Z risepir-rpc mainnet: reconcile at block 25832880: 8 account(s) exact vs independent provider
2026-08-25T15:06:13Z risepir-rpc mainnet: reconcile at block 25832910: 8 account(s) exact vs independent provider
2026-08-25T15:12:40Z risepir-rpc mainnet: reconcile at block 25832940: 8 account(s) exact vs independent provider
2026-08-25T15:19:02Z risepir-rpc mainnet: reconcile at block 25832970: 8 account(s) exact vs independent provider
2026-08-25T15:25:25Z risepir-rpc mainnet: reconcile at block 25833000: 8 account(s) exact vs independent provider
2026-08-25T15:31:47Z risepir-rpc mainnet: reconcile at block 25833030: 8 account(s) exact vs independent provider
2026-08-25T15:38:10Z risepir-rpc mainnet: reconcile at block 25833060: 8 account(s) exact vs independent provider
2026-08-25T15:38:23Z risepir-rpc mainnet: reconcile at block 25833090: 8 account(s) exact vs independent provider
2026-08-25T15:44:47Z risepir-rpc mainnet: reconcile at block 25833120: 8 account(s) exact vs independent provider
2026-08-25T15:51:10Z risepir-rpc mainnet: reconcile at block 25833150: 8 account(s) exact vs independent provider
2026-08-25T15:57:33Z risepir-rpc mainnet: reconcile at block 25833180: 8 account(s) exact vs independent provider
2026-08-25T16:06:22Z risepir-rpc mainnet: reconcile at block 25833210: 8 account(s) exact vs independent provider
2026-08-25T16:10:22Z risepir-rpc mainnet: reconcile at block 25833240: 8 account(s) exact vs independent provider
2026-08-25T16:16:46Z risepir-rpc mainnet: reconcile at block 25833270: 8 account(s) exact vs independent provider
2026-08-25T16:23:04Z risepir-rpc mainnet: reconcile at block 25833300: 8 account(s) exact vs independent provider
2026-08-25T16:29:33Z risepir-rpc mainnet: reconcile at block 25833330: 8 account(s) exact vs independent provider
2026-08-25T16:35:51Z risepir-rpc mainnet: reconcile at block 25833360: 8 account(s) exact vs independent provider
2026-08-25T16:42:14Z risepir-rpc mainnet: reconcile at block 25833390: 8 account(s) exact vs independent provider
2026-08-25T16:48:38Z risepir-rpc mainnet: reconcile at block 25833420: 8 account(s) exact vs independent provider
2026-08-25T16:55:02Z risepir-rpc mainnet: reconcile at block 25833450: 8 account(s) exact vs independent provider
2026-08-25T17:01:26Z risepir-rpc mainnet: reconcile at block 25833480: 8 account(s) exact vs independent provider
2026-08-25T17:07:50Z risepir-rpc mainnet: reconcile at block 25833510: 8 account(s) exact vs independent provider
2026-08-25T17:14:13Z risepir-rpc mainnet: reconcile at block 25833540: 8 account(s) exact vs independent provider
2026-08-25T17:20:36Z risepir-rpc mainnet: reconcile at block 25833570: 8 account(s) exact vs independent provider
2026-08-25T17:20:51Z risepir-rpc mainnet: reconcile at block 25833600: 8 account(s) exact vs independent provider
2026-08-25T17:27:17Z risepir-rpc mainnet: reconcile at block 25833630: 8 account(s) exact vs independent provider
2026-08-25T17:33:38Z risepir-rpc mainnet: reconcile at block 25833660: 8 account(s) exact vs independent provider
2026-08-25T17:40:01Z risepir-rpc mainnet: reconcile at block 25833690: 8 account(s) exact vs independent provider
2026-08-25T17:46:24Z risepir-rpc mainnet: reconcile at block 25833720: 8 account(s) exact vs independent provider
2026-08-25T17:52:44Z risepir-rpc mainnet: reconcile at block 25833750: 8 account(s) exact vs independent provider
2026-08-25T17:59:08Z risepir-rpc mainnet: reconcile at block 25833780: 8 account(s) exact vs independent provider
2026-08-25T18:05:32Z risepir-rpc mainnet: reconcile at block 25833810: 8 account(s) exact vs independent provider
2026-08-25T18:11:56Z risepir-rpc mainnet: reconcile at block 25833840: 8 account(s) exact vs independent provider
2026-08-25T18:18:19Z risepir-rpc mainnet: reconcile at block 25833870: 8 account(s) exact vs independent provider
2026-08-25T18:24:37Z risepir-rpc mainnet: reconcile at block 25833900: 8 account(s) exact vs independent provider
2026-08-25T18:31:00Z risepir-rpc mainnet: reconcile at block 25833930: 8 account(s) exact vs independent provider
2026-08-25T18:37:28Z risepir-rpc mainnet: reconcile at block 25833960: 8 account(s) exact vs independent provider
2026-08-25T18:43:52Z risepir-rpc mainnet: reconcile at block 25833990: 8 account(s) exact vs independent provider
2026-08-25T18:50:15Z risepir-rpc mainnet: reconcile at block 25834020: 8 account(s) exact vs independent provider
2026-08-25T18:56:38Z risepir-rpc mainnet: reconcile at block 25834050: 8 account(s) exact vs independent provider
2026-08-25T19:02:57Z risepir-rpc mainnet: reconcile at block 25834080: 8 account(s) exact vs independent provider
2026-08-25T19:03:12Z risepir-rpc mainnet: reconcile at block 25834110: 8 account(s) exact vs independent provider
2026-08-25T19:09:35Z risepir-rpc mainnet: reconcile at block 25834140: 8 account(s) exact vs independent provider
2026-08-25T19:15:59Z risepir-rpc mainnet: reconcile at block 25834170: 8 account(s) exact vs independent provider
2026-08-25T19:22:23Z risepir-rpc mainnet: reconcile at block 25834200: 8 account(s) exact vs independent provider
2026-08-25T19:28:49Z risepir-rpc mainnet: reconcile at block 25834230: 8 account(s) exact vs independent provider
2026-08-25T19:35:08Z risepir-rpc mainnet: reconcile at block 25834260: 8 account(s) exact vs independent provider
2026-08-25T19:41:33Z risepir-rpc mainnet: reconcile at block 25834290: 8 account(s) exact vs independent provider
2026-08-25T19:47:55Z risepir-rpc mainnet: reconcile at block 25834320: 8 account(s) exact vs independent provider
2026-08-25T19:54:21Z risepir-rpc mainnet: reconcile at block 25834350: 8 account(s) exact vs independent provider
2026-08-25T20:00:44Z risepir-rpc mainnet: reconcile at block 25834380: 8 account(s) exact vs independent provider
2026-08-25T20:07:04Z risepir-rpc mainnet: reconcile at block 25834410: 8 account(s) exact vs independent provider
2026-08-25T20:13:33Z risepir-rpc mainnet: reconcile at block 25834440: 8 account(s) exact vs independent provider
2026-08-25T20:19:51Z risepir-rpc mainnet: reconcile at block 25834470: 8 account(s) exact vs independent provider
2026-08-25T20:26:10Z risepir-rpc mainnet: reconcile at block 25834500: 8 account(s) exact vs independent provider
2026-08-25T20:32:38Z risepir-rpc mainnet: reconcile at block 25834530: 8 account(s) exact vs independent provider
2026-08-25T20:38:57Z risepir-rpc mainnet: reconcile at block 25834560: 8 account(s) exact vs independent provider
2026-08-25T20:39:10Z risepir-rpc mainnet: reconcile at block 25834590: 8 account(s) exact vs independent provider
2026-08-25T20:45:38Z risepir-rpc mainnet: reconcile at block 25834620: 8 account(s) exact vs independent provider
2026-08-25T20:51:58Z risepir-rpc mainnet: reconcile at block 25834650: 8 account(s) exact vs independent provider
2026-08-25T20:58:40Z risepir-rpc mainnet: reconcile at block 25834680: 8 account(s) exact vs independent provider
2026-08-25T21:04:51Z risepir-rpc mainnet: reconcile at block 25834710: 8 account(s) exact vs independent provider
2026-08-25T21:11:12Z risepir-rpc mainnet: reconcile at block 25834740: 8 account(s) exact vs independent provider
2026-08-25T21:17:31Z risepir-rpc mainnet: reconcile at block 25834770: 8 account(s) exact vs independent provider
2026-08-25T21:23:55Z risepir-rpc mainnet: reconcile at block 25834800: 8 account(s) exact vs independent provider
2026-08-25T21:30:24Z risepir-rpc mainnet: reconcile at block 25834830: 8 account(s) exact vs independent provider
2026-08-25T21:36:43Z risepir-rpc mainnet: reconcile at block 25834860: 8 account(s) exact vs independent provider
2026-08-25T21:43:02Z risepir-rpc mainnet: reconcile at block 25834890: 8 account(s) exact vs independent provider
2026-08-25T21:49:25Z risepir-rpc mainnet: reconcile at block 25834920: 8 account(s) exact vs independent provider
2026-08-25T21:55:48Z risepir-rpc mainnet: reconcile at block 25834950: 8 account(s) exact vs independent provider
2026-08-25T22:02:09Z risepir-rpc mainnet: reconcile at block 25834980: 8 account(s) exact vs independent provider
2026-08-25T22:08:40Z risepir-rpc mainnet: reconcile at block 25835010: 8 account(s) exact vs independent provider
2026-08-25T22:15:01Z risepir-rpc mainnet: reconcile at block 25835040: 8 account(s) exact vs independent provider
2026-08-25T22:15:15Z risepir-rpc mainnet: reconcile at block 25835070: 8 account(s) exact vs independent provider
2026-08-25T22:21:39Z risepir-rpc mainnet: reconcile at block 25835100: 8 account(s) exact vs independent provider
2026-08-25T22:27:57Z risepir-rpc mainnet: reconcile at block 25835130: 8 account(s) exact vs independent provider
2026-08-25T22:34:19Z risepir-rpc mainnet: reconcile at block 25835160: 8 account(s) exact vs independent provider
2026-08-25T22:40:47Z risepir-rpc mainnet: reconcile at block 25835190: 8 account(s) exact vs independent provider
2026-08-25T22:47:09Z risepir-rpc mainnet: reconcile at block 25835220: 8 account(s) exact vs independent provider
2026-08-25T22:53:31Z risepir-rpc mainnet: reconcile at block 25835250: 8 account(s) exact vs independent provider
2026-08-25T22:59:52Z risepir-rpc mainnet: reconcile at block 25835280: 8 account(s) exact vs independent provider
2026-08-25T23:06:19Z risepir-rpc mainnet: reconcile at block 25835310: 8 account(s) exact vs independent provider
2026-08-25T23:12:41Z risepir-rpc mainnet: reconcile at block 25835340: 8 account(s) exact vs independent provider
2026-08-25T23:19:09Z risepir-rpc mainnet: reconcile at block 25835370: 8 account(s) exact vs independent provider
2026-08-25T23:25:25Z risepir-rpc mainnet: reconcile at block 25835400: 8 account(s) exact vs independent provider
2026-08-25T23:31:49Z risepir-rpc mainnet: reconcile at block 25835430: 8 account(s) exact vs independent provider
2026-08-25T23:38:13Z risepir-rpc mainnet: reconcile at block 25835460: 8 account(s) exact vs independent provider
2026-08-25T23:44:35Z risepir-rpc mainnet: reconcile at block 25835490: 8 account(s) exact vs independent provider
2026-08-25T23:50:58Z risepir-rpc mainnet: reconcile at block 25835520: 8 account(s) exact vs independent provider
2026-08-25T23:57:20Z risepir-rpc mainnet: reconcile at block 25835550: 8 account(s) exact vs independent provider
2026-08-26T00:03:48Z risepir-rpc mainnet: reconcile at block 25835580: 8 account(s) exact vs independent provider
2026-08-26T00:04:04Z risepir-rpc mainnet: reconcile at block 25835610: 8 account(s) exact vs independent provider
2026-08-26T00:10:21Z risepir-rpc mainnet: reconcile at block 25835640: 8 account(s) exact vs independent provider
2026-08-26T00:16:49Z risepir-rpc mainnet: reconcile at block 25835670: 8 account(s) exact vs independent provider
2026-08-26T00:23:12Z risepir-rpc mainnet: reconcile at block 25835700: 8 account(s) exact vs independent provider
2026-08-26T00:29:32Z risepir-rpc mainnet: reconcile at block 25835730: 8 account(s) exact vs independent provider
2026-08-26T00:35:54Z risepir-rpc mainnet: reconcile at block 25835760: 8 account(s) exact vs independent provider
2026-08-26T00:42:17Z risepir-rpc mainnet: reconcile at block 25835790: 8 account(s) exact vs independent provider
2026-08-26T00:48:42Z risepir-rpc mainnet: reconcile at block 25835820: 8 account(s) exact vs independent provider
2026-08-26T00:55:09Z risepir-rpc mainnet: reconcile at block 25835850: 8 account(s) exact vs independent provider
2026-08-26T01:01:26Z risepir-rpc mainnet: reconcile at block 25835880: 8 account(s) exact vs independent provider
2026-08-26T01:07:55Z risepir-rpc mainnet: reconcile at block 25835910: 8 account(s) exact vs independent provider
2026-08-26T01:14:13Z risepir-rpc mainnet: reconcile at block 25835940: 8 account(s) exact vs independent provider
2026-08-26T01:20:37Z risepir-rpc mainnet: reconcile at block 25835970: 8 account(s) exact vs independent provider
2026-08-26T01:27:00Z risepir-rpc mainnet: reconcile at block 25836000: 8 account(s) exact vs independent provider
2026-08-26T01:33:20Z risepir-rpc mainnet: reconcile at block 25836030: 8 account(s) exact vs independent provider
2026-08-26T01:39:49Z risepir-rpc mainnet: reconcile at block 25836060: 8 account(s) exact vs independent provider
2026-08-26T01:40:03Z risepir-rpc mainnet: reconcile at block 25836090: 8 account(s) exact vs independent provider
2026-08-26T01:46:23Z risepir-rpc mainnet: reconcile at block 25836120: 8 account(s) exact vs independent provider
2026-08-26T01:52:49Z risepir-rpc mainnet: reconcile at block 25836150: 8 account(s) exact vs independent provider
2026-08-26T01:59:09Z risepir-rpc mainnet: reconcile at block 25836180: 8 account(s) exact vs independent provider
2026-08-26T02:05:36Z risepir-rpc mainnet: reconcile at block 25836210: 8 account(s) exact vs independent provider
2026-08-26T02:11:57Z risepir-rpc mainnet: reconcile at block 25836240: 8 account(s) exact vs independent provider
2026-08-26T02:18:20Z risepir-rpc mainnet: reconcile at block 25836270: 8 account(s) exact vs independent provider
2026-08-26T02:24:41Z risepir-rpc mainnet: reconcile at block 25836300: 8 account(s) exact vs independent provider
2026-08-26T02:31:07Z risepir-rpc mainnet: reconcile at block 25836330: 8 account(s) exact vs independent provider
2026-08-26T02:37:30Z risepir-rpc mainnet: reconcile at block 25836360: 8 account(s) exact vs independent provider
2026-08-26T02:43:51Z risepir-rpc mainnet: reconcile at block 25836390: 8 account(s) exact vs independent provider
2026-08-26T02:50:12Z risepir-rpc mainnet: reconcile at block 25836420: 8 account(s) exact vs independent provider
2026-08-26T02:56:39Z risepir-rpc mainnet: reconcile at block 25836450: 8 account(s) exact vs independent provider
2026-08-26T03:03:00Z risepir-rpc mainnet: reconcile at block 25836480: 8 account(s) exact vs independent provider
2026-08-26T03:09:31Z risepir-rpc mainnet: reconcile at block 25836510: 8 account(s) exact vs independent provider
2026-08-26T03:15:47Z risepir-rpc mainnet: reconcile at block 25836540: 8 account(s) exact vs independent provider
2026-08-26T03:22:09Z risepir-rpc mainnet: reconcile at block 25836570: 8 account(s) exact vs independent provider
2026-08-26T03:22:23Z risepir-rpc mainnet: reconcile at block 25836600: 8 account(s) exact vs independent provider
2026-08-26T03:28:50Z risepir-rpc mainnet: reconcile at block 25836630: 8 account(s) exact vs independent provider
2026-08-26T03:35:10Z risepir-rpc mainnet: reconcile at block 25836660: 8 account(s) exact vs independent provider
2026-08-26T03:41:30Z risepir-rpc mainnet: reconcile at block 25836690: 8 account(s) exact vs independent provider
2026-08-26T03:47:55Z risepir-rpc mainnet: reconcile at block 25836720: 8 account(s) exact vs independent provider
2026-08-26T03:54:16Z risepir-rpc mainnet: reconcile at block 25836750: 8 account(s) exact vs independent provider
2026-08-26T04:00:42Z risepir-rpc mainnet: reconcile at block 25836780: 8 account(s) exact vs independent provider
2026-08-26T04:07:07Z risepir-rpc mainnet: reconcile at block 25836810: 8 account(s) exact vs independent provider
2026-08-26T04:13:30Z risepir-rpc mainnet: reconcile at block 25836840: 8 account(s) exact vs independent provider
2026-08-26T04:19:53Z risepir-rpc mainnet: reconcile at block 25836870: 8 account(s) exact vs independent provider
2026-08-26T04:26:13Z risepir-rpc mainnet: reconcile at block 25836900: 8 account(s) exact vs independent provider
2026-08-26T04:32:41Z risepir-rpc mainnet: reconcile at block 25836930: 8 account(s) exact vs independent provider
2026-08-26T04:39:01Z risepir-rpc mainnet: reconcile at block 25836960: 8 account(s) exact vs independent provider
2026-08-26T04:45:24Z risepir-rpc mainnet: reconcile at block 25836990: 8 account(s) exact vs independent provider
2026-08-26T04:51:52Z risepir-rpc mainnet: reconcile at block 25837020: 8 account(s) exact vs independent provider
2026-08-26T04:58:13Z risepir-rpc mainnet: reconcile at block 25837050: 8 account(s) exact vs independent provider
2026-08-26T04:58:25Z risepir-rpc mainnet: reconcile at block 25837080: 8 account(s) exact vs independent provider
2026-08-26T05:04:53Z risepir-rpc mainnet: reconcile at block 25837110: 8 account(s) exact vs independent provider
2026-08-26T05:11:10Z risepir-rpc mainnet: reconcile at block 25837140: 8 account(s) exact vs independent provider
2026-08-26T05:17:33Z risepir-rpc mainnet: reconcile at block 25837170: 8 account(s) exact vs independent provider
2026-08-26T05:24:00Z risepir-rpc mainnet: reconcile at block 25837200: 8 account(s) exact vs independent provider
2026-08-26T05:30:21Z risepir-rpc mainnet: reconcile at block 25837230: 8 account(s) exact vs independent provider
2026-08-26T05:36:41Z risepir-rpc mainnet: reconcile at block 25837260: 8 account(s) exact vs independent provider
2026-08-26T05:43:08Z risepir-rpc mainnet: reconcile at block 25837290: 8 account(s) exact vs independent provider
2026-08-26T05:49:32Z risepir-rpc mainnet: reconcile at block 25837320: 8 account(s) exact vs independent provider
2026-08-26T05:55:57Z risepir-rpc mainnet: reconcile at block 25837350: 8 account(s) exact vs independent provider
2026-08-26T06:02:13Z risepir-rpc mainnet: reconcile at block 25837380: 8 account(s) exact vs independent provider
2026-08-26T06:08:38Z risepir-rpc mainnet: reconcile at block 25837410: 8 account(s) exact vs independent provider
2026-08-26T06:15:05Z risepir-rpc mainnet: reconcile at block 25837440: 8 account(s) exact vs independent provider
2026-08-26T06:21:24Z risepir-rpc mainnet: reconcile at block 25837470: 8 account(s) exact vs independent provider
2026-08-26T06:27:49Z risepir-rpc mainnet: reconcile at block 25837500: 8 account(s) exact vs independent provider
2026-08-26T06:34:09Z risepir-rpc mainnet: reconcile at block 25837530: 8 account(s) exact vs independent provider
2026-08-26T06:40:36Z risepir-rpc mainnet: reconcile at block 25837560: 8 account(s) exact vs independent provider
2026-08-26T06:40:51Z risepir-rpc mainnet: reconcile at block 25837590: 8 account(s) exact vs independent provider
2026-08-26T06:47:13Z risepir-rpc mainnet: reconcile at block 25837620: 8 account(s) exact vs independent provider
2026-08-26T06:53:33Z risepir-rpc mainnet: reconcile at block 25837650: 8 account(s) exact vs independent provider
2026-08-26T06:59:55Z risepir-rpc mainnet: reconcile at block 25837680: 8 account(s) exact vs independent provider
2026-08-26T07:06:22Z risepir-rpc mainnet: reconcile at block 25837710: 8 account(s) exact vs independent provider
2026-08-26T07:12:42Z risepir-rpc mainnet: reconcile at block 25837740: 8 account(s) exact vs independent provider
2026-08-26T07:19:08Z risepir-rpc mainnet: reconcile at block 25837770: 8 account(s) exact vs independent provider
2026-08-26T07:25:29Z risepir-rpc mainnet: reconcile at block 25837800: 8 account(s) exact vs independent provider
2026-08-26T07:31:54Z risepir-rpc mainnet: reconcile at block 25837830: 8 account(s) exact vs independent provider
2026-08-26T07:38:13Z risepir-rpc mainnet: reconcile at block 25837860: 8 account(s) exact vs independent provider
2026-08-26T07:44:38Z risepir-rpc mainnet: reconcile at block 25837890: 8 account(s) exact vs independent provider
2026-08-26T07:51:04Z risepir-rpc mainnet: reconcile at block 25837920: 8 account(s) exact vs independent provider
2026-08-26T07:57:25Z risepir-rpc mainnet: reconcile at block 25837950: 8 account(s) exact vs independent provider
2026-08-26T08:03:53Z risepir-rpc mainnet: reconcile at block 25837980: 8 account(s) exact vs independent provider
2026-08-26T08:10:10Z risepir-rpc mainnet: reconcile at block 25838010: 8 account(s) exact vs independent provider
2026-08-26T08:16:32Z risepir-rpc mainnet: reconcile at block 25838040: 8 account(s) exact vs independent provider
2026-08-26T08:22:57Z risepir-rpc mainnet: reconcile at block 25838070: 8 account(s) exact vs independent provider
2026-08-26T08:23:11Z risepir-rpc mainnet: reconcile at block 25838100: 8 account(s) exact vs independent provider
2026-08-26T08:29:32Z risepir-rpc mainnet: reconcile at block 25838130: 8 account(s) exact vs independent provider
2026-08-26T08:36:00Z risepir-rpc mainnet: reconcile at block 25838160: 8 account(s) exact vs independent provider
2026-08-26T08:42:22Z risepir-rpc mainnet: reconcile at block 25838190: 8 account(s) exact vs independent provider
2026-08-26T08:48:43Z risepir-rpc mainnet: reconcile at block 25838220: 8 account(s) exact vs independent provider
2026-08-26T08:55:10Z risepir-rpc mainnet: reconcile at block 25838250: 8 account(s) exact vs independent provider
2026-08-26T09:01:33Z risepir-rpc mainnet: reconcile at block 25838280: 8 account(s) exact vs independent provider
2026-08-26T09:07:53Z risepir-rpc mainnet: reconcile at block 25838310: 8 account(s) exact vs independent provider
2026-08-26T09:14:21Z risepir-rpc mainnet: reconcile at block 25838340: 8 account(s) exact vs independent provider
2026-08-26T09:20:40Z risepir-rpc mainnet: reconcile at block 25838370: 8 account(s) exact vs independent provider
2026-09-02T17:34:46Z risepir-rpc mainnet: state loaded in 113.4s — block 25838386, 203879841 accounts, complete set
2026-09-02T17:43:02Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 20 checkpoint(s) (~600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T17:51:04Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 40 checkpoint(s) (~1200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T17:58:15Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 60 checkpoint(s) (~1800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T18:07:04Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 80 checkpoint(s) (~2400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T18:16:05Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 100 checkpoint(s) (~3000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T18:24:09Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 120 checkpoint(s) (~3600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T18:33:45Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 140 checkpoint(s) (~4200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T18:44:00Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 160 checkpoint(s) (~4800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T18:53:32Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 180 checkpoint(s) (~5400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:07:23Z risepir-rpc mainnet: state loaded in 6.3s — block 25844151, 203953291 accounts, complete set
2026-09-02T19:08:55Z risepir-rpc mainnet: state loaded in 7.1s — block 25844151, 203953291 accounts, complete set
2026-09-02T19:12:51Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 20 checkpoint(s) (~600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:15:14Z risepir-rpc mainnet: state loaded in 6.2s — block 25844769, 203964204 accounts, complete set
2026-09-02T19:19:51Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 20 checkpoint(s) (~600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:22:35Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 40 checkpoint(s) (~1200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:24:46Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 60 checkpoint(s) (~1800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:26:46Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 80 checkpoint(s) (~2400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:29:00Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 100 checkpoint(s) (~3000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:32:09Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 120 checkpoint(s) (~3600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:36:30Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 140 checkpoint(s) (~4200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:39:06Z risepir-rpc mainnet: state loaded in 6.2s — block 25849049, 204052162 accounts, complete set
2026-09-02T19:43:05Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 20 checkpoint(s) (~600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:47:30Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 40 checkpoint(s) (~1200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:50:28Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 60 checkpoint(s) (~1800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:53:16Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 80 checkpoint(s) (~2400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:55:45Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 100 checkpoint(s) (~3000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T19:57:59Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 120 checkpoint(s) (~3600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:00:23Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 140 checkpoint(s) (~4200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:02:53Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 160 checkpoint(s) (~4800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:05:22Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 180 checkpoint(s) (~5400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:08:19Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 200 checkpoint(s) (~6000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:10:44Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 220 checkpoint(s) (~6600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:13:11Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 240 checkpoint(s) (~7200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:15:57Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 260 checkpoint(s) (~7800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:18:12Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 280 checkpoint(s) (~8400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:20:35Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 300 checkpoint(s) (~9000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:23:04Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 320 checkpoint(s) (~9600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:25:43Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 340 checkpoint(s) (~10200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:27:54Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 360 checkpoint(s) (~10800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:30:14Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 380 checkpoint(s) (~11400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:32:24Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 400 checkpoint(s) (~12000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:35:24Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 420 checkpoint(s) (~12600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:38:44Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 440 checkpoint(s) (~13200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:41:40Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 460 checkpoint(s) (~13800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:44:47Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 480 checkpoint(s) (~14400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:47:19Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 500 checkpoint(s) (~15000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:49:21Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 520 checkpoint(s) (~15600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:51:35Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 540 checkpoint(s) (~16200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:53:55Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 560 checkpoint(s) (~16800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:56:25Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 580 checkpoint(s) (~17400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T20:58:32Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 600 checkpoint(s) (~18000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:00:40Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 620 checkpoint(s) (~18600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:03:12Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 640 checkpoint(s) (~19200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:05:42Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 660 checkpoint(s) (~19800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:08:14Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 680 checkpoint(s) (~20400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:11:00Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 700 checkpoint(s) (~21000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:13:36Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 720 checkpoint(s) (~21600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:15:52Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 740 checkpoint(s) (~22200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:18:21Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 760 checkpoint(s) (~22800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:20:36Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 780 checkpoint(s) (~23400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:22:26Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 800 checkpoint(s) (~24000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:24:42Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 820 checkpoint(s) (~24600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:26:51Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 840 checkpoint(s) (~25200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:28:59Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 860 checkpoint(s) (~25800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:31:39Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 880 checkpoint(s) (~26400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:33:57Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 900 checkpoint(s) (~27000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:37:33Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 920 checkpoint(s) (~27600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:41:14Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 940 checkpoint(s) (~28200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:45:30Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 960 checkpoint(s) (~28800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:48:54Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 980 checkpoint(s) (~29400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:51:50Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1000 checkpoint(s) (~30000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:54:43Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1020 checkpoint(s) (~30600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T21:57:46Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1040 checkpoint(s) (~31200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:00:00Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1060 checkpoint(s) (~31800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:02:34Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1080 checkpoint(s) (~32400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:05:08Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1100 checkpoint(s) (~33000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:07:25Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1120 checkpoint(s) (~33600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:11:06Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1140 checkpoint(s) (~34200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:13:42Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1160 checkpoint(s) (~34800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:16:21Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1180 checkpoint(s) (~35400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:19:04Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1200 checkpoint(s) (~36000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:21:21Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1220 checkpoint(s) (~36600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:23:53Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1240 checkpoint(s) (~37200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:26:15Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1260 checkpoint(s) (~37800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:28:43Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1280 checkpoint(s) (~38400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:30:47Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1300 checkpoint(s) (~39000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:33:41Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1320 checkpoint(s) (~39600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:36:12Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1340 checkpoint(s) (~40200 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:39:02Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1360 checkpoint(s) (~40800 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:42:01Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1380 checkpoint(s) (~41400 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:44:28Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1400 checkpoint(s) (~42000 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak was DEFERRED, not failed: this deployment is further behind the finalized head than the keyless reference provider serves, so no request has been sent to it at all. This is the expected shape of a deep catch-up (a re-bootstrap, or a long outage) and it clears by itself once the replay reaches the provider's window — no operator action, and no reason to suspect the provider; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:47:20Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1420 checkpoint(s) (~42600 blocks) — no cross-provider comparison has succeeded since block 0 — every checkpoint in that streak attempted at least one comparison and every attempt failed — the independent reconcile provider appears unavailable; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:49:57Z risepir-rpc mainnet: CRITICAL: the reconcile integrity backstop has been dark for 1440 checkpoint(s) (~43200 blocks) — no cross-provider comparison has succeeded since block 0 — that streak mixes deferred checkpoints (too far behind the reference provider to ask) with attempted-and-failed ones, so both catch-up lag and a provider problem are in play; following continues regardless (a third-party outage must not become this deployment's outage), but the ingest path is running unverified until this clears
2026-09-02T22:51:30Z risepir-rpc mainnet: reconcile at block 25892610: 8 account(s) exact vs independent provider
2026-09-02T22:54:53Z risepir-rpc mainnet: state loaded in 7.2s — block 25892623, 204713227 accounts, complete set
2026-09-02T22:55:04Z risepir-rpc mainnet: reconcile at block 25892640: 8 account(s) exact vs independent provider
2026-09-02T22:59:51Z risepir-rpc mainnet: reconcile at block 25892670: 8 account(s) exact vs independent provider
2026-09-02T23:06:09Z risepir-rpc mainnet: reconcile at block 25892700: 8 account(s) exact vs independent provider
2026-09-02T23:12:37Z risepir-rpc mainnet: reconcile at block 25892730: 8 account(s) exact vs independent provider
2026-09-02T23:19:04Z risepir-rpc mainnet: reconcile at block 25892760: 8 account(s) exact vs independent provider
2026-09-02T23:25:23Z risepir-rpc mainnet: reconcile at block 25892790: 8 account(s) exact vs independent provider
2026-09-02T23:31:49Z risepir-rpc mainnet: reconcile at block 25892820: 8 account(s) exact vs independent provider
2026-09-02T23:38:09Z risepir-rpc mainnet: reconcile at block 25892850: 8 account(s) exact vs independent provider
2026-09-02T23:44:33Z risepir-rpc mainnet: reconcile at block 25892880: 8 account(s) exact vs independent provider
2026-09-02T23:44:43Z risepir-rpc mainnet: reconcile at block 25892910: 8 account(s) exact vs independent provider
2026-09-02T23:51:08Z risepir-rpc mainnet: reconcile at block 25892940: 8 account(s) exact vs independent provider
2026-09-02T23:57:34Z risepir-rpc mainnet: reconcile at block 25892970: 8 account(s) exact vs independent provider
2026-09-03T00:03:59Z risepir-rpc mainnet: reconcile at block 25893000: 8 account(s) exact vs independent provider
2026-09-03T00:10:18Z risepir-rpc mainnet: reconcile at block 25893030: 8 account(s) exact vs independent provider
2026-09-03T00:16:47Z risepir-rpc mainnet: reconcile at block 25893060: 8 account(s) exact vs independent provider
2026-09-03T00:23:08Z risepir-rpc mainnet: reconcile at block 25893090: 8 account(s) exact vs independent provider
2026-09-03T00:29:27Z risepir-rpc mainnet: reconcile at block 25893120: 8 account(s) exact vs independent provider
2026-09-03T00:35:52Z risepir-rpc mainnet: reconcile at block 25893150: 8 account(s) exact vs independent provider
2026-09-03T00:42:14Z risepir-rpc mainnet: reconcile at block 25893180: 8 account(s) exact vs independent provider
2026-09-03T00:48:45Z risepir-rpc mainnet: reconcile at block 25893210: 8 account(s) exact vs independent provider
2026-09-03T00:55:01Z risepir-rpc mainnet: reconcile at block 25893240: 8 account(s) exact vs independent provider
2026-09-03T01:01:28Z risepir-rpc mainnet: reconcile at block 25893270: 8 account(s) exact vs independent provider
2026-09-03T01:07:53Z risepir-rpc mainnet: reconcile at block 25893300: 8 account(s) exact vs independent provider
2026-09-03T01:14:09Z risepir-rpc mainnet: reconcile at block 25893330: 8 account(s) exact vs independent provider
2026-09-03T01:20:36Z risepir-rpc mainnet: reconcile at block 25893360: 8 account(s) exact vs independent provider
2026-09-03T01:26:57Z risepir-rpc mainnet: reconcile at block 25893390: 8 account(s) exact vs independent provider
2026-09-03T01:27:08Z risepir-rpc mainnet: reconcile at block 25893420: 8 account(s) exact vs independent provider
2026-09-03T01:33:35Z risepir-rpc mainnet: reconcile at block 25893450: 8 account(s) exact vs independent provider
2026-09-03T01:39:55Z risepir-rpc mainnet: reconcile at block 25893480: 8 account(s) exact vs independent provider
2026-09-03T01:46:24Z risepir-rpc mainnet: reconcile at block 25893510: 8 account(s) exact vs independent provider
2026-09-03T01:52:49Z risepir-rpc mainnet: reconcile at block 25893540: 8 account(s) exact vs independent provider
2026-09-03T01:59:26Z risepir-rpc mainnet: reconcile at block 25893570: 8 account(s) exact vs independent provider
2026-09-03T02:05:30Z risepir-rpc mainnet: reconcile at block 25893600: 8 account(s) exact vs independent provider
2026-09-03T02:11:57Z risepir-rpc mainnet: reconcile at block 25893630: 8 account(s) exact vs independent provider
2026-09-03T02:18:22Z risepir-rpc mainnet: reconcile at block 25893660: 8 account(s) exact vs independent provider
2026-09-03T02:24:48Z risepir-rpc mainnet: reconcile at block 25893690: 8 account(s) exact vs independent provider
2026-09-03T02:31:05Z risepir-rpc mainnet: reconcile at block 25893720: 8 account(s) exact vs independent provider
2026-09-03T02:37:28Z risepir-rpc mainnet: reconcile at block 25893750: 8 account(s) exact vs independent provider
2026-09-03T02:43:51Z risepir-rpc mainnet: reconcile at block 25893780: 8 account(s) exact vs independent provider
2026-09-03T02:50:13Z risepir-rpc mainnet: reconcile at block 25893810: 8 account(s) exact vs independent provider
2026-09-03T02:56:35Z risepir-rpc mainnet: reconcile at block 25893840: 8 account(s) exact vs independent provider
2026-09-03T03:03:01Z risepir-rpc mainnet: reconcile at block 25893870: 8 account(s) exact vs independent provider
2026-09-03T03:03:26Z risepir-rpc mainnet: reconcile at block 25893900: 8 account(s) exact vs independent provider
2026-09-03T03:09:34Z risepir-rpc mainnet: reconcile at block 25893930: 8 account(s) exact vs independent provider
2026-09-03T03:16:01Z risepir-rpc mainnet: reconcile at block 25893960: 8 account(s) exact vs independent provider
2026-09-03T03:22:20Z risepir-rpc mainnet: reconcile at block 25893990: 8 account(s) exact vs independent provider
2026-09-03T03:28:46Z risepir-rpc mainnet: reconcile at block 25894020: 8 account(s) exact vs independent provider
2026-09-03T03:35:53Z risepir-rpc mainnet: reconcile at block 25894050: 8 account(s) exact vs independent provider
2026-09-03T03:41:33Z risepir-rpc mainnet: reconcile at block 25894080: 8 account(s) exact vs independent provider
2026-09-03T03:48:11Z risepir-rpc mainnet: reconcile at block 25894110: 8 account(s) exact vs independent provider
2026-09-03T03:54:18Z risepir-rpc mainnet: reconcile at block 25894140: 8 account(s) exact vs independent provider
2026-09-03T04:00:43Z risepir-rpc mainnet: reconcile at block 25894170: 8 account(s) exact vs independent provider
